# Role metadata and dependencies.
galaxy_info:
  author: Tim Shand
  description: Common configuration for all hosts.
  license: MIT # License under which the role is distributed.
  min_ansible_version: "2.20" # # Minimum Ansible version required to run this role.
  platforms:
    - name: Ubuntu
      versions:
        - noble # 24.04 LTS
        - resolute # 26.04 LTS
    - name: EL
      versions:
        - "9"
        - "10"

# Roles that will execute before this role runs.
# dependencies:
#   - role: common
#   - role: firewall
#     vars:
#       allowed_ports:
#         - 80
#         - 443
