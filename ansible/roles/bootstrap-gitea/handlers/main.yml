---
- name: reload systemd
  ansible.builtin.systemd:
    daemon_reload: true

- name: restart gitea
  ansible.builtin.systemd:
    name: gitea
    state: restarted

- name: restart gitea-runner
  ansible.builtin.systemd:
    name: gitea-runner
    state: restarted
